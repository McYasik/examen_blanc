- Pourquoi le validator ne prends pas en compte la règle "interval:1-20" ?
        => ne rentre donc pas dans le else if associé.

- Bonus : lier à un user ?


-----


La vérification de "l'unique" du livre se fait sur la base du couple nom / auteur.
    -> il peut exister des livres avec le même nom ! Mais l'auteur sera différent.